# import requests

# from requests.auth import HTTPBasicAuth
# import json

# url = "https://harshakp06.atlassian.net/rest/api/3/project"

# export jira1=

numbers = [1, 2, 3, 4]
doubled_numbers = list(map(lambda x: x * 2, numbers))

print(doubled_numbers)

data = [(1, 'apple'), (3, 'llbanana'), (2, 'orange'), (2, 'corange'),(2, 'dorange')]
sorted_data = sorted(data, key=lambda x: x[0])

print(sorted_data)